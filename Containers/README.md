# Example query for the model once its running

```bash
curl http://localhost:11434/api/generate -d '{
  "model": "qwen3:8b",
  "prompt": "Why is the sky blue?"
}'
```

`curl -X 'GET' 'https://obsidian:27124/' -H 'accept: application/json' -H 'Authorization: Bearer API_KEY_HERE'`
